# SPDX-License-Identifier: MPL-2.0
#
# User-level systemd service for cosmic-nightshift's background daemon.
#
# Install with:
#   mkdir -p ~/.config/systemd/user
#   cp systemd/cosmic-nightshift.service ~/.config/systemd/user/
#   systemctl --user daemon-reload
#   systemctl --user enable --now cosmic-nightshift.service
#
# Adjust ExecStart below if the binary isn't installed to /usr/local/bin.

[Unit]
Description=Cosmic Night Shift background service
After=graphical-session.target

[Service]
ExecStart=/usr/local/bin/cosmic-nightshift --daemon
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
